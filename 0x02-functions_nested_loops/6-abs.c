#include "main.h"

/**
 * _abs - Function to get absolute values
 *
 * @r: The number
 *
 * Return: int number indicating success or otherwise
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
